#!/usr/bin/env bash
# Run rofi app launcher
i3-dmenu-desktop --dmenu=" \
  rofi \
    -theme 'fullscreen' \
    -dmenu -i -p 'apps'"

    # -show run \
    # -eh 2 \
    # -theme-str 'listview {lines: 5;}' \
